<ul data-role="listview" data-inset="true">
  <% @books.each do |book| %>
    <li>
    <%= link_to book.title, book %>
    </li>
  <% end %>
</ul>

<%= link_to 'New Book', new_book_path, :class => 'ui-btn'  %>
